---
phase: 09-ui-parceiros-riscos-gov-indicadores
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/supcdt/GovernancaSection.tsx
  - src/components/supcdt/IndicadoresSection.tsx
  - src/components/supcdt/DetalheProjeto.tsx
autonomous: true
requirements:
  - UIGN-01
  - UIGN-02
  - UIIN-01
  - UIIN-02

must_haves:
  truths:
    - "GovernancaSection renders chronological list of decisions"
    - "Nova decisao button opens compact modal and saves via POST API"
    - "IndicadoresSection renders horizontal bars per data series"
    - "All four sections (Parceiros, Riscos, Governanca, Indicadores) are wired into DetalheProjeto"
    - "Each section only renders when its respective modulosAtivos flag is true"
  artifacts:
    - path: "src/components/supcdt/GovernancaSection.tsx"
      provides: "Governanca decisions UI section component"
      exports: ["GovernancaSection"]
    - path: "src/components/supcdt/IndicadoresSection.tsx"
      provides: "Research indicators UI section component"
      exports: ["IndicadoresSection"]
    - path: "src/components/supcdt/DetalheProjeto.tsx"
      provides: "Updated project detail with all 4 module sections wired"
      contains: "ParceirosSection"
  key_links:
    - from: "src/components/supcdt/DetalheProjeto.tsx"
      to: "src/components/supcdt/ParceirosSection.tsx"
      via: "import and render"
      pattern: "import.*ParceirosSection"
    - from: "src/components/supcdt/DetalheProjeto.tsx"
      to: "src/components/supcdt/RiscosSection.tsx"
      via: "import and render"
      pattern: "import.*RiscosSection"
    - from: "src/components/supcdt/DetalheProjeto.tsx"
      to: "src/components/supcdt/GovernancaSection.tsx"
      via: "import and render"
      pattern: "import.*GovernancaSection"
    - from: "src/components/supcdt/DetalheProjeto.tsx"
      to: "src/components/supcdt/IndicadoresSection.tsx"
      via: "import and render"
      pattern: "import.*IndicadoresSection"
---

<objective>
Create GovernancaSection and IndicadoresSection components, then wire all 4 module sections into DetalheProjeto.

Purpose: Complete the Phase 9 UI surface — all 4 monitoring modules visible and interactive in the project detail page.
Output: 2 new section components + updated DetalheProjeto with all 4 sections wired.
</objective>

<execution_context>
@/Users/hrocha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/projeto.ts (DecisaoGovernanca, IndicadorPesquisa, SerieDados, ModulosAtivos)
@src/lib/api/governanca.ts (API module from 09-01)
@src/lib/api/indicadores.ts (API module from 09-01)
@src/components/supcdt/DetalheProjeto.tsx (target for wiring)
@src/components/supcdt/EtapasSection.tsx (reference pattern from Phase 8)
@src/components/supcdt/ParceirosSection.tsx (sibling from 09-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GovernancaSection component</name>
  <files>src/components/supcdt/GovernancaSection.tsx</files>
  <action>
Create `src/components/supcdt/GovernancaSection.tsx` that receives `{ projeto: Projeto; onUpdate: () => void }`.

**Conditional visibility:** If `projeto.modulosAtivos?.governanca` is not `true`, return `null`.

**Layout:** Same card pattern:
- Header with label "Estrategia" (violet-700/70) + title "Decisoes de Governanca"
- "Nova decisao" button in header

**Decisions list (UIGN-01):** Sort `projeto.decisoes` by `data` descending (most recent first). Each decisao shows:
- Data formatted as DD/MM/YYYY (text-xs text-slate-400, left side or top)
- Titulo (text-sm font-semibold text-slate-950)
- Descricao (text-xs text-slate-600, truncated to 2 lines with `line-clamp-2`)
- Responsavel badge (text-xs, if present)
- Delete button (Trash2 icon)

**Nova decisao modal (UIGN-02):** Button in header opens a compact modal (NOT inline form — use Dialog from shadcn/ui). Fields:
- titulo (text input, required)
- data (date input, required, default to today)
- descricao (textarea, optional)
- responsavel (text input, optional)
On submit, call `createDecisao(projeto.id, data, token)` then `onUpdate()` and close modal.

Use `useAuth()` for token. Use `toast` for errors.
  </action>
  <verify>
    <automated>cd /Users/hrocha/Projetos/Secti/dashboard-SUPCDT/.claude/worktrees/recursing-gagarin && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <done>GovernancaSection renders chronological decision list with compact modal for new decisions. Returns null when modulosAtivos.governanca is not true.</done>
</task>

<task type="auto">
  <name>Task 2: Create IndicadoresSection component</name>
  <files>src/components/supcdt/IndicadoresSection.tsx</files>
  <action>
Create `src/components/supcdt/IndicadoresSection.tsx` that receives `{ projeto: Projeto; onUpdate: () => void }`.

**Conditional visibility:** If `projeto.modulosAtivos?.indicadores` is not `true`, return `null`.

**Layout:** Same card pattern:
- Header with label "Pesquisa" (teal-700/70) + title "Indicadores de Pesquisa"
- "Novo indicador" button in header

**Indicators list (UIIN-01):** Each indicador shows:
- Nome (text-sm font-semibold text-slate-950)
- Categoria badge (text-xs, if present)
- Horizontal bars for each SerieDados entry:
  - Label on the left (text-xs text-slate-600)
  - Bar width proportional to `valor / maxValor` of the series (use the max value in the series as 100%)
  - Value on the right (text-xs text-slate-950)
  - Bar color: teal-500 (or use a subtle gradient)
- Delete button per indicator

**Novo indicador (UIIN-02):** Inline form (collapsible, same pattern as EtapasSection). Fields:
- nome (text input, required)
- categoria (text input, optional)
- Initial series: At least one row with label + valor inputs. Allow adding more series rows with a "+" button.
On submit, call `createIndicador(projeto.id, { nome, categoria, series }, token)` then `onUpdate()`.

Use `useAuth()` for token. Use `toast` for errors.
  </action>
  <verify>
    <automated>cd /Users/hrocha/Projetos/Secti/dashboard-SUPCDT/.claude/worktrees/recursing-gagarin && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <done>IndicadoresSection renders indicators with horizontal bar charts per series. Returns null when modulosAtivos.indicadores is not true.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all 4 sections into DetalheProjeto</name>
  <files>src/components/supcdt/DetalheProjeto.tsx</files>
  <action>
Update `src/components/supcdt/DetalheProjeto.tsx` to import and render the 4 new module sections.

**Note:** Phase 8 already wired EtapasSection and OrcamentoSection. This task adds the remaining 4.

**Imports to add:**
```ts
import { ParceirosSection } from './ParceirosSection';
import { RiscosSection } from './RiscosSection';
import { GovernancaSection } from './GovernancaSection';
import { IndicadoresSection } from './IndicadoresSection';
```

**Rendering:** Add the 4 sections in the content area of DetalheProjeto, after the existing EtapasSection and OrcamentoSection (from Phase 8). Each section manages its own visibility via modulosAtivos flags, so no wrapping conditional is needed in DetalheProjeto.

Place them in logical order:
1. EtapasSection (Phase 8)
2. OrcamentoSection (Phase 8)
3. ParceirosSection
4. RiscosSection
5. GovernancaSection
6. IndicadoresSection

Each receives `projeto={projeto} onUpdate={handleRefresh}` where `handleRefresh` is the existing function that reloads project data.

If `handleRefresh` or equivalent doesn't exist yet, create a function that calls the project fetch API and updates local state.
  </action>
  <verify>
    <automated>cd /Users/hrocha/Projetos/Secti/dashboard-SUPCDT/.claude/worktrees/recursing-gagarin && grep -c "Section" src/components/supcdt/DetalheProjeto.tsx</automated>
  </verify>
  <done>DetalheProjeto imports and renders all 4 new module sections. Each section receives projeto and onUpdate props. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. GovernancaSection exports and checks modulosAtivos.governanca
3. IndicadoresSection exports and checks modulosAtivos.indicadores
4. DetalheProjeto imports all 4 sections (grep for ParceirosSection, RiscosSection, GovernancaSection, IndicadoresSection)
5. All sections receive projeto and onUpdate props
</verification>

<success_criteria>
- GovernancaSection with chronological list and modal for new decisions
- IndicadoresSection with horizontal bars per series
- All 4 sections wired into DetalheProjeto
- Each section self-manages visibility via modulosAtivos
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-parceiros-riscos-gov-indicadores/09-02-SUMMARY.md`
</output>
