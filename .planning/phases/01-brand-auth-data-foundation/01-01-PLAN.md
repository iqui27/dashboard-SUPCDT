---
phase: 01-brand-auth-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Login.tsx
  - src/components/Header.tsx
  - src/App.tsx
  - src/components/ProtectedRoute.tsx
  - src/index.css
autonomous: true
requirements: [AUTH-01, AUTH-02, BRAND-01, UX-01]

must_haves:
  truths:
    - "Login do SUPCDT deixa de parecer reaproveitado de outro dashboard"
    - "Nenhuma ação de acesso automático permanece visível para o usuário"
    - "Header e navegação funcionam bem em desktop e mobile"
    - "A shell transmite produto institucional maduro desde a primeira tela"
  artifacts:
    - path: "src/components/Login.tsx"
      provides: "login rebrandado, sem botão de bypass"
    - path: "src/components/Header.tsx"
      provides: "header responsivo com navegação consistente"
    - path: "src/index.css"
      provides: "tokens e base visual coerentes para a nova identidade"
---

<objective>
Personalizar a experiência de entrada e a shell principal antes das demais entregas.

Purpose: A primeira impressão do produto precisa ser profissional e segura. O usuário não pode ver atalhos de admin nem uma interface genérica.
Output: login rebrandado, remoção do bypass, header/navigation responsivos e shell coerente.
</objective>

<context>
@src/components/Login.tsx
@src/components/Header.tsx
@src/App.tsx
@src/components/ProtectedRoute.tsx
@src/index.css
@.planning/phases/01-brand-auth-data-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand do login SUPCDT</name>
  <files>src/components/Login.tsx, src/index.css</files>
  <action>
  Redesenhar a tela de login com identidade própria do SUPCDT:
  - estética minimalista institucional contemporânea
  - composição memorável e não genérica
  - layout responsivo mobile-first
  - mensagem de acesso clara e sóbria
  - estados de erro, loading e foco refinados

  O resultado não pode parecer um clone do outro dashboard.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
    <manual>Em 390px e desktop, o login parece produto final e não template</manual>
  </verify>
  <done>Login visualmente distinto, responsivo e coerente com a identidade do SUPCDT.</done>
</task>

<task type="auto">
  <name>Task 2: Remover bypass e endurecer a entrada</name>
  <files>src/components/Login.tsx, src/components/ProtectedRoute.tsx, src/AppRouter.tsx</files>
  <action>
  Eliminar qualquer affordance de acesso sem autenticação:
  - remover o botão "Testar sem login (Admin)"
  - revisar textos e estados para deixar claro que o acesso depende de credencial
  - validar que rotas privadas continuam protegidas
  - evitar qualquer CTA que simule login automático
  </action>
  <verify>
    <automated>rg -n "Testar sem login|admin123|handleTestLogin" src</automated>
    <manual>Usuário sem token sempre cai no fluxo de login</manual>
  </verify>
  <done>Nenhum atalho de acesso sem autenticação permanece na UI.</done>
</task>

<task type="auto">
  <name>Task 3: Header e navegação responsivos</name>
  <files>src/components/Header.tsx, src/App.tsx</files>
  <action>
  Evoluir o header para uso real:
  - navegação mobile clara, sem depender apenas de tabs desktop escondidas
  - branding coerente com o novo login
  - feedback visual de aba atual
  - área do usuário mais limpa e institucional
  - comportamento estável nas telas de detalhe e na home autenticada
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
    <manual>Em mobile, é possível navegar sem ambiguidade entre as áreas principais</manual>
  </verify>
  <done>Header funciona bem em mobile e desktop, com identidade unificada.</done>
</task>

</tasks>

<verification>
1. `npm run lint` sem erros
2. `npm run build` sem erros
3. Busca textual não encontra bypass de login na UI
4. Login e header estão coerentes visualmente entre si
</verification>

<success_criteria>
- Login com identidade própria do SUPCDT
- Nenhum acesso "sem login" disponível
- Header responsivo e profissional
- Shell inicial pronta para uso institucional
</success_criteria>

<output>
Criar `.planning/phases/01-brand-auth-data-foundation/01-01-SUMMARY.md`
</output>
