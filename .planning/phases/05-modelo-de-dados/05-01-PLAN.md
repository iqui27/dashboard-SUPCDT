---
phase: 05-modelo-de-dados
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/projeto.ts
  - server/types/projeto.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - DATA-10

must_haves:
  truths:
    - "npx tsc --noEmit compila sem erros após as alterações"
    - "Um objeto Projeto sem campo modulosAtivos é TypeScript válido (todos os flags são opcionais)"
    - "Os tipos Etapa, Entregavel, RubricaOrcamentaria, AditivoRubrica, Parceiro, Risco, DecisaoGovernanca, IndicadorPesquisa e ModulosAtivos existem e são exportados em ambos os arquivos"
    - "normalizeProjeto() no frontend preserva modulosAtivos sem alterar seu valor (spread passthrough)"
  artifacts:
    - path: "src/types/projeto.ts"
      provides: "Tipos de módulo frontend + modulosAtivos? em Projeto e ProjetoInput"
      exports:
        - Entregavel
        - Etapa
        - AditivoRubrica
        - RubricaOrcamentaria
        - Parceiro
        - Risco
        - DecisaoGovernanca
        - IndicadorPesquisa
        - ModulosAtivos
    - path: "server/types/projeto.ts"
      provides: "Tipos de módulo backend + modulosAtivos? em DBProjeto, ProjetoApi e ProjetoInput"
      exports:
        - DBEntregavel
        - DBEtapa
        - DBAditivoRubrica
        - DBRubricaOrcamentaria
        - DBParceiro
        - DBRisco
        - DBDecisaoGovernanca
        - DBIndicadorPesquisa
        - DBModulosAtivos
  key_links:
    - from: "src/types/projeto.ts"
      to: "Projeto.modulosAtivos"
      via: "campo opcional ModulosAtivos | undefined"
      pattern: "modulosAtivos\\?.*ModulosAtivos"
    - from: "server/types/projeto.ts"
      to: "DBProjeto.modulosAtivos"
      via: "campo opcional DBModulosAtivos | undefined"
      pattern: "modulosAtivos\\?.*DBModulosAtivos"
---

<objective>
Definir todos os tipos TypeScript necessários para os 6 módulos de monitoramento avançado (Etapas, Orçamento, Parceiros, Riscos, Governança, Indicadores) em ambos os arquivos de tipos do projeto, mantendo retrocompatibilidade total com projetos existentes no MongoDB.

Purpose: Phase 6 (API Backend) depende diretamente destes tipos. Sem eles, nenhum endpoint de módulo pode ser implementado com type-safety.

Output: `src/types/projeto.ts` e `server/types/projeto.ts` atualizados com 9 novos tipos exportados cada um e campo `modulosAtivos?` adicionado a `Projeto`, `ProjetoInput`, `DBProjeto`, `ProjetoApi` e `ProjetoInput` do backend.
</objective>

<execution_context>
@/Users/hrocha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/projeto.ts
@server/types/projeto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tipos de módulos ao frontend (src/types/projeto.ts)</name>
  <files>src/types/projeto.ts</files>
  <action>
Adicionar os seguintes tipos ANTES da interface `Projeto`, logo após `MovimentacaoHistorico` (linha ~46), mantendo a ordem de dependência (tipos folha primeiro):

```typescript
// ─── Módulo: Etapas ──────────────────────────────────────────────────────────

export interface Entregavel {
  id: string;
  nome: string;
  concluido: boolean;
}

export interface Etapa {
  id: string;
  nome: string;
  percentual: number; // 0-100
  entregaveis: Entregavel[];
}

// ─── Módulo: Orçamento ───────────────────────────────────────────────────────

export interface AditivoRubrica {
  id: string;
  descricao: string;
  valor: number;
  data?: string | null; // ISO date string
}

export interface RubricaOrcamentaria {
  id: string;
  nome: string;
  previsto: number;
  executado: number;
  aditivos: AditivoRubrica[];
}

// ─── Módulo: Parceiros ───────────────────────────────────────────────────────

export type StatusParceiro = 'Ativo' | 'Apoiador' | 'Consultor' | 'Inativo';

export interface Parceiro {
  id: string;
  nome: string;
  papel: string;
  status: StatusParceiro;
}

// ─── Módulo: Riscos ──────────────────────────────────────────────────────────

export type ProbabilidadeRisco = 'Baixa' | 'Média' | 'Alta';
export type ImpactoRisco = 'Baixo' | 'Médio' | 'Alto';
export type StatusRisco = 'Aberto' | 'Mitigado' | 'Encerrado';

export interface Risco {
  id: string;
  descricao: string;
  probabilidade: ProbabilidadeRisco;
  impacto: ImpactoRisco;
  mitigacao?: string | null;
  status: StatusRisco;
}

// ─── Módulo: Governança ──────────────────────────────────────────────────────

export interface DecisaoGovernanca {
  id: string;
  titulo: string;
  data: string; // ISO date string
  descricao?: string | null;
  responsavel?: string | null;
}

// ─── Módulo: Indicadores de Pesquisa ────────────────────────────────────────

export interface SerieDados {
  label: string;
  valor: number;
}

export interface IndicadorPesquisa {
  id: string;
  nome: string;
  categoria: string;
  serie: SerieDados[];
}

// ─── Configuração de Módulos ─────────────────────────────────────────────────

export interface ModulosAtivos {
  etapas?: boolean | undefined;
  orcamento?: boolean | undefined;
  parceiros?: boolean | undefined;
  riscos?: boolean | undefined;
  governanca?: boolean | undefined;
  indicadores?: boolean | undefined;
}
```

Em seguida, adicionar os campos de módulos à interface `Projeto` (após `overrideLastSyncedAt?`):

```typescript
  // ─── Módulos de Monitoramento Avançado (opcionais — retrocompatível) ──────
  modulosAtivos?: ModulosAtivos;
  etapas?: Etapa[];
  rubricas?: RubricaOrcamentaria[];
  parceirosModulo?: Parceiro[];
  riscos?: Risco[];
  decisoes?: DecisaoGovernanca[];
  indicadores?: IndicadorPesquisa[];
```

Adicionar os mesmos campos opcionais à interface `ProjetoInput`:

```typescript
  modulosAtivos?: ModulosAtivos;
  etapas?: Etapa[];
  rubricas?: RubricaOrcamentaria[];
  parceirosModulo?: Parceiro[];
  riscos?: Risco[];
  decisoes?: DecisaoGovernanca[];
  indicadores?: IndicadorPesquisa[];
```

Não alterar nenhuma função existente. O spread em `normalizeProjeto()` já preserva campos desconhecidos via `...projeto`, portanto `modulosAtivos` e arrays de módulos passam automaticamente sem código adicional.

Nota: o campo `parceiro` já existe em `Projeto` como `string | null` (legado). Usar `parceirosModulo` para o array do módulo evita conflito de nome.
  </action>
  <verify>
    <automated>cd /Users/hrocha/Projetos/Secti/dashboard-SUPCDT/.claude/worktrees/recursing-gagarin && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirmar que os novos tipos aparecem no arquivo e que nenhuma função existente foi modificada</manual>
  </verify>
  <done>
    - `src/types/projeto.ts` exporta `Entregavel`, `Etapa`, `AditivoRubrica`, `RubricaOrcamentaria`, `StatusParceiro`, `Parceiro`, `ProbabilidadeRisco`, `ImpactoRisco`, `StatusRisco`, `Risco`, `DecisaoGovernanca`, `SerieDados`, `IndicadorPesquisa`, `ModulosAtivos`
    - `Projeto` e `ProjetoInput` têm `modulosAtivos?`, `etapas?`, `rubricas?`, `parceirosModulo?`, `riscos?`, `decisoes?`, `indicadores?` todos opcionais
    - `tsc --noEmit` passa sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Espelhar tipos de módulos no backend (server/types/projeto.ts)</name>
  <files>server/types/projeto.ts</files>
  <action>
Adicionar os tipos de módulos prefixados com `DB` ao arquivo do backend, logo após `DBProjetoMonitoramento` e antes de `DBProjeto`. Adicionar também os campos de módulos às interfaces existentes:

```typescript
// ─── Módulo: Etapas ──────────────────────────────────────────────────────────

export interface DBEntregavel {
  id: string;
  nome: string;
  concluido: boolean;
}

export interface DBEtapa {
  id: string;
  nome: string;
  percentual: number; // 0-100
  entregaveis: DBEntregavel[];
}

// ─── Módulo: Orçamento ───────────────────────────────────────────────────────

export interface DBAditivoRubrica {
  id: string;
  descricao: string;
  valor: number;
  data?: Date | null;
}

export interface DBRubricaOrcamentaria {
  id: string;
  nome: string;
  previsto: number;
  executado: number;
  aditivos: DBAditivoRubrica[];
}

// ─── Módulo: Parceiros ───────────────────────────────────────────────────────

export type DBStatusParceiro = 'Ativo' | 'Apoiador' | 'Consultor' | 'Inativo';

export interface DBParceiro {
  id: string;
  nome: string;
  papel: string;
  status: DBStatusParceiro;
}

// ─── Módulo: Riscos ──────────────────────────────────────────────────────────

export type DBProbabilidadeRisco = 'Baixa' | 'Média' | 'Alta';
export type DBImpactoRisco = 'Baixo' | 'Médio' | 'Alto';
export type DBStatusRisco = 'Aberto' | 'Mitigado' | 'Encerrado';

export interface DBRisco {
  id: string;
  descricao: string;
  probabilidade: DBProbabilidadeRisco;
  impacto: DBImpactoRisco;
  mitigacao?: string | null;
  status: DBStatusRisco;
}

// ─── Módulo: Governança ──────────────────────────────────────────────────────

export interface DBDecisaoGovernanca {
  id: string;
  titulo: string;
  data: Date;
  descricao?: string | null;
  responsavel?: string | null;
}

// ─── Módulo: Indicadores de Pesquisa ────────────────────────────────────────

export interface DBSerieDados {
  label: string;
  valor: number;
}

export interface DBIndicadorPesquisa {
  id: string;
  nome: string;
  categoria: string;
  serie: DBSerieDados[];
}

// ─── Configuração de Módulos ─────────────────────────────────────────────────

export interface DBModulosAtivos {
  etapas?: boolean | undefined;
  orcamento?: boolean | undefined;
  parceiros?: boolean | undefined;
  riscos?: boolean | undefined;
  governanca?: boolean | undefined;
  indicadores?: boolean | undefined;
}
```

Em seguida, adicionar os campos opcionais à interface `DBProjeto` (após `updatedAt?`):

```typescript
  // ─── Módulos de Monitoramento Avançado (opcionais — retrocompatível) ──────
  modulosAtivos?: DBModulosAtivos;
  etapas?: DBEtapa[];
  rubricas?: DBRubricaOrcamentaria[];
  parceirosModulo?: DBParceiro[];
  riscos?: DBRisco[];
  decisoes?: DBDecisaoGovernanca[];
  indicadores?: DBIndicadorPesquisa[];
```

Adicionar os mesmos campos à interface `ProjetoApi` (após `updatedAt?`):

```typescript
  modulosAtivos?: DBModulosAtivos;
  etapas?: DBEtapa[];
  rubricas?: DBRubricaOrcamentaria[];
  parceirosModulo?: DBParceiro[];
  riscos?: DBRisco[];
  decisoes?: DBDecisaoGovernanca[];
  indicadores?: DBIndicadorPesquisa[];
```

Adicionar os mesmos campos à interface `ProjetoInput` do backend (após os campos de compatibilidade temporária):

```typescript
  modulosAtivos?: DBModulosAtivos;
  etapas?: DBEtapa[];
  rubricas?: DBRubricaOrcamentaria[];
  parceirosModulo?: DBParceiro[];
  riscos?: DBRisco[];
  decisoes?: DBDecisaoGovernanca[];
  indicadores?: DBIndicadorPesquisa[];
```

Diferença do frontend: `DBAditivoRubrica.data` é `Date | null` (MongoDB armazena Date nativo); `DBDecisaoGovernanca.data` é `Date` (sem string). A camada de API converte para string ISO ao serializar para o frontend.
  </action>
  <verify>
    <automated>cd /Users/hrocha/Projetos/Secti/dashboard-SUPCDT/.claude/worktrees/recursing-gagarin && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirmar que server/types/projeto.ts exporta todos os tipos DB* e que DBProjeto, ProjetoApi e ProjetoInput têm os campos de módulo opcionais</manual>
  </verify>
  <done>
    - `server/types/projeto.ts` exporta `DBEntregavel`, `DBEtapa`, `DBAditivoRubrica`, `DBRubricaOrcamentaria`, `DBStatusParceiro`, `DBParceiro`, `DBProbabilidadeRisco`, `DBImpactoRisco`, `DBStatusRisco`, `DBRisco`, `DBDecisaoGovernanca`, `DBSerieDados`, `DBIndicadorPesquisa`, `DBModulosAtivos`
    - `DBProjeto`, `ProjetoApi` e `ProjetoInput` têm todos os campos de módulo opcionais
    - `tsc --noEmit` passa sem erros (frontend + backend)
    - Nenhuma interface ou função existente foi alterada ou removida
  </done>
</task>

</tasks>

<verification>
Após completar os dois tasks, executar verificação final:

```bash
cd /Users/hrocha/Projetos/Secti/dashboard-SUPCDT/.claude/worktrees/recursing-gagarin && npx tsc --noEmit
```

Saída esperada: nenhuma linha de erro. Exit code 0.

Verificação de retrocompatibilidade (sem execução de servidor necessária):
- Confirmar que `Projeto` sem `modulosAtivos` é válido: todos os novos campos têm `?` (opcional)
- Confirmar que `DBProjeto` sem `modulosAtivos` é válido: idem
- Confirmar que `normalizeProjeto()` em src/types/projeto.ts usa `...projeto` spread e não lista campos explicitamente — portanto `modulosAtivos` e arrays de módulos passam automaticamente
</verification>

<success_criteria>
1. `npx tsc --noEmit` completa sem erros (exit code 0)
2. `src/types/projeto.ts` contém 14 novos exports de tipos de módulo e `ModulosAtivos`
3. `server/types/projeto.ts` contém 14 novos exports prefixados com `DB`
4. Campo `modulosAtivos?: ModulosAtivos` presente em `Projeto` e `ProjetoInput` do frontend
5. Campo `modulosAtivos?: DBModulosAtivos` presente em `DBProjeto`, `ProjetoApi` e `ProjetoInput` do backend
6. Nenhuma função existente foi modificada
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-modelo-de-dados/05-01-SUMMARY.md` com:
- Tipos adicionados (lista completa)
- Decisão de nomenclatura (DB* prefix no backend, campo parceirosModulo para evitar conflito)
- Resultado do tsc --noEmit
- Quaisquer ajustes feitos durante a execução
</output>
