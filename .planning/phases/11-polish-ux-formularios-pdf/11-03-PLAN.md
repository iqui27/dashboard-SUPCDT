---
phase: 11-polish-ux-formularios-pdf
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/components/supcdt/DetalheProjeto.tsx
  - src/components/supcdt/MetasChart.tsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04]

must_haves:
  truths:
    - "Stats bar do DetalheProjeto mostra semáforos coloridos (verde/amarelo/vermelho) ao lado de cada indicador (status operacional, nível de risco, saúde da entrega)"
    - "Quando uma meta tem realizadoTotal > totalPrevisto e totalPrevisto > 0, aparece badge 'Meta superada' em emerald com o percentual real"
    - "Seção de metas no DetalheProjeto inclui gráfico de barras Recharts mostrando Previsto vs Realizado por trimestre para cada meta"
    - "Painel operacional exibe timestamp formatado de 'Última atualização do monitoramento'"
    - "Progresso físico na stats bar não ultrapassa 100% visualmente (barra capped), mas exibe o valor real em texto"
  artifacts:
    - path: "src/components/supcdt/DetalheProjeto.tsx"
      provides: "Semáforos na stats bar + Meta Superada badge + timestamp + progresso capped"
      contains: "semaforo"
    - path: "src/components/supcdt/MetasChart.tsx"
      provides: "Gráfico Recharts de barras Previsto vs Realizado por trimestre"
      contains: "BarChart"
      min_lines: 40
  key_links:
    - from: "src/components/supcdt/MetasChart.tsx"
      to: "recharts"
      via: "import { BarChart, Bar, XAxis, YAxis } from recharts"
      pattern: "BarChart"
    - from: "src/components/supcdt/DetalheProjeto.tsx"
      to: "src/components/supcdt/MetasChart.tsx"
      via: "import MetasChart"
      pattern: "MetasChart"
---

<objective>
Adicionar indicadores visuais semáforo na stats bar, badges de "Meta Superada", gráficos de barras Recharts comparando Previsto vs Realizado por trimestre, e timestamp de última atualização no painel operacional.

Purpose: O gestor precisa de leitura visual rápida — cores de semáforo para saber se o projeto está bem sem ler texto, gráficos para comparação temporal, e saber quando o monitoramento foi atualizado pela última vez.
Output: Stats bar com semáforos coloridos, badges em metas superadas, gráfico MetasChart.tsx, timestamp no painel operacional.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-ux-formularios-pdf/11-02-SUMMARY.md
@src/components/supcdt/DetalheProjeto.tsx
@src/types/projeto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semáforos na stats bar + Meta Superada badge + Progresso capped + Timestamp</name>
  <files>
    src/components/supcdt/DetalheProjeto.tsx
  </files>
  <action>
    **1. Função helper de semáforo** — adicionar no início do arquivo (ou inline):

    Criar uma função `getSemaforoCor(label: string, value: string): string` que retorna a classe de cor de um círculo indicador:

    - **Status operacional**:
      - "Operando" → `bg-emerald-500`
      - "Planejado", "Em implantação" → `bg-amber-400`
      - "Atenção", "Crítico", "Encerrado" → `bg-rose-500`

    - **Nível de risco**:
      - "Baixo" → `bg-emerald-500`
      - "Médio" → `bg-amber-400`
      - "Alto", "Crítico" → `bg-rose-500`

    - **Saúde da entrega**:
      - "Saudável" → `bg-emerald-500`
      - "Observação" → `bg-amber-400`
      - "Risco" → `bg-rose-500`

    Default (sem dado / "Não definido"): `bg-slate-300`

    **2. Aplicar semáforos na stats bar** — na primeira grid (4 colunas com Status atual, Status operacional, Nível de risco, Saúde da entrega):

    Alterar o markup de cada item para incluir um dot colorido ANTES do valor:
    ```tsx
    <div className="flex items-center gap-2">
      <span className={`h-2 w-2 shrink-0 rounded-full ${getSemaforoCor(item.label, item.value)}`} />
      <p className={`text-sm font-bold ${item.flag ? 'text-rose-700' : 'text-slate-900'}`}>{item.value}</p>
    </div>
    ```

    Para "Status atual" (que é diferente), usar:
    - "Em andamento", "Assinado" → `bg-emerald-500`
    - "Suspenso" → `bg-amber-400`
    - "Encerrado" → `bg-slate-400`

    **3. Meta Superada badge** — na renderização de cada meta (dentro da lista de metas):

    Após a linha que mostra `{meta.realizadoTotal} / {meta.totalPrevisto}`, adicionar condição:
    ```tsx
    {meta.totalPrevisto > 0 && meta.realizadoTotal > meta.totalPrevisto && (
      <span className="mt-1 inline-block rounded-full bg-emerald-100 px-2 py-0.5 text-[10px] font-semibold text-emerald-700">
        Meta superada ({percentual.toFixed(0)}%)
      </span>
    )}
    ```

    Também: quando meta superada, trocar a cor da barra de progresso de `bg-sky-600` para `bg-emerald-500`.

    **4. Progresso físico capped** — na stats bar, o "Progresso físico" atualmente mostra `{progresso.toFixed(0)}%` (pode mostrar 600%). Manter o texto como está (valor real), mas a BARRA já está capped com `Math.min(100, ...)`. Porém, quando >100%, adicionar indicador visual:
    ```tsx
    <p className={`mt-0.5 text-sm font-bold ${progresso > 100 ? 'text-emerald-700' : 'text-slate-900'}`}>
      {progresso.toFixed(0)}%
      {progresso > 100 && <span className="ml-1 text-[10px] font-normal text-emerald-600">Acima do previsto</span>}
    </p>
    ```

    **5. Timestamp "Última atualização"** — no painel operacional (coluna direita), adicionar como PRIMEIRO item da lista:
    ```tsx
    {
      label: 'Última atualização',
      value: operacional.ultimaAtualizacao
        ? new Date(operacional.ultimaAtualizacao).toLocaleString('pt-BR', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' })
        : 'Nunca atualizado'
    }
    ```

    IMPORTANTE: NÃO alterar a estrutura de nenhum endpoint. Apenas alterar o frontend (DetalheProjeto.tsx). O campo `operacional.ultimaAtualizacao` já é salvo pelo ProjetoMonitoramentoModal.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Abrir DetalheProjeto — stats bar mostra dots coloridos ao lado de cada status. Meta com realizado > previsto mostra badge verde "Meta superada". Painel operacional mostra data/hora da última atualização.</manual>
  </verify>
  <done>Stats bar tem semáforos coloridos para todos os 4 indicadores da primeira linha; metas superadas têm badge emerald com percentual; barra de progresso muda de cor quando >100%; progresso >100% mostra texto "Acima do previsto"; painel operacional mostra timestamp formatado</done>
</task>

<task type="auto">
  <name>Task 2: Gráfico Recharts Previsto vs Realizado por trimestre</name>
  <files>
    src/components/supcdt/MetasChart.tsx
    src/components/supcdt/DetalheProjeto.tsx
  </files>
  <action>
    **src/components/supcdt/MetasChart.tsx** — criar novo componente:

    Gráfico de barras agrupadas (Grouped Bar Chart) usando Recharts, mostrando Previsto vs Realizado por trimestre para UMA meta selecionada.

    Props:
    ```typescript
    interface MetasChartProps {
      meta: Meta;
      totalTrimestres: number;
    }
    ```

    Transformar dados da meta em formato Recharts:
    ```typescript
    const data = Array.from({ length: totalTrimestres }, (_, idx) => ({
      name: `T${idx + 1}`,
      previsto: meta.previstoPorTrimestre[idx] || 0,
      realizado: meta.realizadoPorTrimestre[idx] || 0,
    }));
    ```

    Layout do gráfico:
    - `<ResponsiveContainer width="100%" height={160}>`
    - `<BarChart data={data} barGap={2} barSize={18}>`
    - Duas barras: Previsto (fill `#cbd5e1` = slate-300) e Realizado (fill `#0ea5e9` = sky-500)
    - XAxis com `dataKey="name"`, fontSize 11, tick fill `#94a3b8`
    - YAxis oculto para limpeza visual (allowDecimals={false})
    - `<Tooltip>` com contentStyle customizado: `{ borderRadius: '0.75rem', fontSize: '12px', border: '1px solid #e2e8f0' }`
    - `<Legend>` na parte inferior com fontSize 11
    - Sem grid lines (cartesianGrid={false} ou não incluir)

    Quando Realizado > Previsto em algum trimestre, a barra Realizado fica verde (`#10b981` = emerald-500) em vez de sky. Implementar via `fill` dinâmico no `<Bar>`:
    ```tsx
    <Bar dataKey="realizado" name="Realizado" radius={[4, 4, 0, 0]}>
      {data.map((entry, index) => (
        <Cell key={index} fill={entry.realizado > entry.previsto ? '#10b981' : '#0ea5e9'} />
      ))}
    </Bar>
    ```

    Import necessário: `import { BarChart, Bar, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer, Cell } from 'recharts';`

    **src/components/supcdt/DetalheProjeto.tsx** — wiring do gráfico:

    1. Importar `MetasChart` do novo arquivo
    2. Na renderização de cada meta (dentro do `<li>` com a grid de trimestres), ABAIXO da grid de T1-T4, adicionar:
       ```tsx
       {projeto.cronograma.totalTrimestres > 0 && (
         <div className="mt-3">
           <MetasChart meta={meta} totalTrimestres={projeto.cronograma.totalTrimestres} />
         </div>
       )}
       ```
    3. O gráfico aparece apenas quando há trimestres configurados (totalTrimestres > 0) — mesma condição que já existe para a grid de T1-T4.

    IMPORTANTE: Recharts já está instalado (`recharts: ^2.12.7`). NÃO instalar nada novo. Não alterar nenhum endpoint de API.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Abrir DetalheProjeto com projeto que tenha metas com previsto e realizado por trimestre — gráfico de barras aparece abaixo de cada meta, barras azuis/verdes conforme previsto vs realizado.</manual>
  </verify>
  <done>MetasChart.tsx criado com gráfico Recharts de barras agrupadas; gráfico renderizado abaixo de cada meta no DetalheProjeto; cores dinâmicas (azul quando dentro do previsto, verde quando supera); tooltip funcional; responsivo</done>
</task>

</tasks>

<verification>
- Stats bar mostra dots coloridos ao lado de cada status
- Semáforo verde para "Operando"/"Baixo"/"Saudável", amarelo para "Em implantação"/"Médio"/"Observação", vermelho para "Crítico"/"Alto"/"Risco"
- Meta com realizadoTotal > totalPrevisto mostra badge "Meta superada" em verde
- Barra de progresso muda de cor para emerald quando meta é superada
- Gráfico Recharts aparece abaixo de cada meta com barras Previsto vs Realizado
- Progresso físico >100% mostra texto "Acima do previsto"
- Painel operacional mostra timestamp da última atualização
- TypeScript compila sem erros
</verification>

<success_criteria>
- Leitura visual rápida via semáforos na stats bar para os 4 indicadores
- Metas superadas têm feedback visual positivo (badge + cor da barra)
- Gráfico temporal Recharts compara Previsto vs Realizado por trimestre
- Timestamp de última atualização visível no painel operacional
- Zero impacto em endpoints de API
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-ux-formularios-pdf/11-03-SUMMARY.md`
</output>
