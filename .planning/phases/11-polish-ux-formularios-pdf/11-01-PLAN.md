---
phase: 11-polish-ux-formularios-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/lancamentos.ts
  - server/services/lancamentos.ts
  - src/lib/api/lancamentos.ts
  - src/components/supcdt/EditarProjetoModal.tsx
  - src/components/supcdt/DetalheProjeto.tsx
autonomous: true
requirements: [EDIT-01, EDIT-03, EDIT-04]

must_haves:
  truths:
    - "Gestor consegue editar dados básicos do projeto (nome, OSC, status, valor, datas) via modal no DetalheProjeto"
    - "Backend aceita PUT e DELETE em /api/lancamentos/:id"
    - "Backend aceita dataAtividade como campo opcional no POST e PUT de lançamentos"
    - "Frontend API layer tem funções updateLancamento e deleteLancamento prontas para uso"
  artifacts:
    - path: "server/routes/lancamentos.ts"
      provides: "PUT /api/lancamentos/:id + DELETE /api/lancamentos/:id"
      contains: "router.put"
    - path: "server/services/lancamentos.ts"
      provides: "updateLancamento + deleteLancamento functions"
      contains: "updateLancamento"
    - path: "src/lib/api/lancamentos.ts"
      provides: "updateLancamento + deleteLancamento client functions"
      contains: "updateLancamento"
    - path: "src/components/supcdt/EditarProjetoModal.tsx"
      provides: "Modal para edição dos dados básicos do projeto"
      min_lines: 80
    - path: "src/components/supcdt/DetalheProjeto.tsx"
      provides: "Botão de edição no hero + import do EditarProjetoModal"
      contains: "EditarProjetoModal"
  key_links:
    - from: "src/components/supcdt/EditarProjetoModal.tsx"
      to: "/api/projetos/:id"
      via: "updateProjeto from src/lib/api/projetos.ts"
      pattern: "updateProjeto"
    - from: "src/lib/api/lancamentos.ts"
      to: "/api/lancamentos/:id"
      via: "fetch PUT/DELETE"
      pattern: "fetch.*lancamentos"
---

<objective>
Adicionar backend CRUD completo para lançamentos (editar/deletar) e criar modal de edição de dados básicos do projeto.

Purpose: Gestor precisa corrigir dados errados sem ter que deletar e recriar. Lançamentos precisam aceitar data de atividade customizada.
Output: Endpoints PUT/DELETE lançamentos funcionando, API layer atualizada, EditarProjetoModal integrado no DetalheProjeto.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@server/routes/lancamentos.ts
@server/services/lancamentos.ts
@src/lib/api/lancamentos.ts
@src/lib/api/projetos.ts
@src/components/supcdt/DetalheProjeto.tsx
@src/components/supcdt/CriacaoProjetoWizard.tsx
@src/types/projeto.ts
@server/types/projeto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend PUT/DELETE lançamentos + aceitar dataAtividade</name>
  <files>
    server/routes/lancamentos.ts
    server/services/lancamentos.ts
    src/lib/api/lancamentos.ts
  </files>
  <action>
    **server/services/lancamentos.ts** — adicionar 2 funções:
    
    1. `updateLancamento(id: string, updateData: Partial<DBLancamento>)`:
       - Recebe id + campos a atualizar
       - Usa `findOneAndUpdate` com `$set` na collection `lancamentos_supcdt`
       - Não permite alterar `_id`, `projetoId`, `createdAt`
       - Após salvar, chama `recalculateProjetoMetas(projetoId)` para manter metas sincronizadas
       - Retorna o documento atualizado
    
    2. `deleteLancamento(id: string)`:
       - Busca o lançamento primeiro para obter o `projetoId`
       - Deleta da collection `lancamentos_supcdt`
       - Após deletar, chama `recalculateProjetoMetas(projetoId)` para recalcular metas
       - Retorna boolean de sucesso
    
    3. Na função `createLancamento` existente, garantir que `dataAtividade` (string ISO opcional) é preservado no documento se presente no payload.
    
    **server/routes/lancamentos.ts** — adicionar 2 rotas:
    
    1. `PUT /api/lancamentos/:id` (requireAuth):
       - Valida ObjectId
       - Extrai campos editáveis do body: `trimestre`, `descricaoAtividade`, `localAtendido`, `valores`, `dataAtividade`
       - Chama `lancamentosService.updateLancamento(id, campos)`
       - Retorna 200 com o lançamento atualizado
    
    2. `DELETE /api/lancamentos/:id` (requireAuth):
       - Valida ObjectId
       - Chama `lancamentosService.deleteLancamento(id)`
       - Retorna 204 no sucesso, 404 se não encontrar
    
    **src/lib/api/lancamentos.ts** — adicionar 2 funções client:
    
    1. `updateLancamento(id: string, data: Partial<Lancamento>, token: string)` — PUT
    2. `deleteLancamento(id: string, token: string)` — DELETE
    
    IMPORTANTE: Usar apenas endpoints em `/api/lancamentos` (database `dashboard_supcdt`). NÃO mexer em nenhuma rota do outro dashboard.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.server.json 2>/dev/null || npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>curl -X PUT com body JSON para /api/lancamentos/:id retorna 200; curl -X DELETE retorna 204</manual>
  </verify>
  <done>PUT e DELETE de lançamentos funcionam via API; dataAtividade é aceito em POST e PUT; recalculateProjetoMetas é chamado automaticamente após cada operação; API layer frontend exporta updateLancamento e deleteLancamento</done>
</task>

<task type="auto">
  <name>Task 2: EditarProjetoModal + botão de edição no DetalheProjeto</name>
  <files>
    src/components/supcdt/EditarProjetoModal.tsx
    src/components/supcdt/DetalheProjeto.tsx
  </files>
  <action>
    **src/components/supcdt/EditarProjetoModal.tsx** — criar novo componente:
    
    Modal para editar os campos básicos do projeto. Usar o mesmo visual dos modais existentes (MetaModal, LancamentoModal como referência):
    - rounded-[1.5rem], backdrop-blur, sticky header com ícone + título
    - Campos editáveis:
      - `nome` (text input, obrigatório)
      - `nomeOSC` (text input)
      - `status` (Select: "Em andamento", "Assinado", "Encerrado", "Suspenso")
      - `categoria` (Select: "Emenda", "INEX", "Convênio", "Recurso Próprio", "Outro")
      - `responsavelSECTI` (text input)
      - `parceiro` (text input)
      - `numeroTermo` (text input)
      - `processoSEI` (text input)
      - `valorTotal` (number input, formatado como moeda)
      - `dataInicio` (input type="date")
      - `dataFim` (input type="date")
      - `descricao` (textarea)
      - `objetivos` (textarea)
      - `raPerigao` (text input — território/região)
    - Organizar em grid 2 colunas (sm:grid-cols-2) para inputs curtos
    - Inicializar todos os fields com valores atuais do projeto
    - Submit via `updateProjeto(projeto.id, updatedFields, token)` — já existe em `src/lib/api/projetos.ts`
    - Toast success/error via sonner
    - Props: `{ projeto: Projeto; onClose: () => void; onSuccess: () => void }`
    
    **src/components/supcdt/DetalheProjeto.tsx** — wiring:
    
    1. Importar `EditarProjetoModal`
    2. Adicionar state `isEditarProjetoOpen`
    3. Na seção de botões do hero (ao lado do gear icon), adicionar botão:
       ```
       <Button variant="outline" className="h-8 rounded-full ..." onClick={() => setIsEditarProjetoOpen(true)}>
         <Pencil className="mr-1.5 h-3 w-3" />
         Editar projeto
       </Button>
       ```
    4. Renderizar `EditarProjetoModal` condicionalmente quando `isEditarProjetoOpen` é true
    
    IMPORTANTE: Usar `updateProjeto` de `src/lib/api/projetos.ts` que já faz PUT em `/api/projetos/:id` (database `dashboard_supcdt`). NÃO usar endpoints do outro dashboard.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Abrir DetalheProjeto, clicar "Editar projeto", alterar nome, salvar — nome atualiza na tela</manual>
  </verify>
  <done>Botão "Editar projeto" aparece no hero do DetalheProjeto; modal abre com todos os campos preenchidos; salvar atualiza o projeto via API e recarrega a tela; campos obrigatórios validados antes do submit</done>
</task>

</tasks>

<verification>
- `PUT /api/lancamentos/:id` com body `{ "descricaoAtividade": "teste" }` retorna 200
- `DELETE /api/lancamentos/:id` retorna 204
- EditarProjetoModal abre, edita nome do projeto, salva com sucesso
- Nenhum endpoint em `/api/projects`, `/api/data-sources`, etc. foi modificado
- TypeScript compila sem erros
</verification>

<success_criteria>
- Backend CRUD completo para lançamentos (GET já existia, POST já existia, PUT e DELETE novos)
- EditarProjetoModal funcional com todos os campos do projeto
- API layer frontend com funções para edit/delete de lançamentos
- Zero impacto nos endpoints do outro dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-ux-formularios-pdf/11-01-SUMMARY.md`
</output>
