---
phase: 11-polish-ux-formularios-pdf
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - src/components/supcdt/ProjetoPDFExport.tsx
  - src/components/supcdt/DetalheProjeto.tsx
  - src/components/supcdt/Relatorios.tsx
  - src/components/supcdt/ProjetoMonitoramentoModal.tsx
autonomous: true
requirements: [PDF-01, PDF-02, MON-FLOW-01]

must_haves:
  truths:
    - "Gestor clica 'Exportar PDF' no DetalheProjeto e um PDF com dados do projeto, metas, progresso e operacional é gerado para download"
    - "Botão PDF no DetalheProjeto está habilitado (não mais 'PDF indisponível')"
    - "Página Relatórios tem botão funcional para gerar PDF institucional de um projeto selecionado"
    - "ProjetoMonitoramentoModal tem organização melhorada com seções visuais claras"
  artifacts:
    - path: "src/components/supcdt/ProjetoPDFExport.tsx"
      provides: "Componente de layout print-friendly + função de geração PDF"
      min_lines: 80
      contains: "window.print"
    - path: "src/components/supcdt/DetalheProjeto.tsx"
      provides: "Botão PDF habilitado que abre preview de impressão"
      contains: "handleExportPDF"
    - path: "src/components/supcdt/Relatorios.tsx"
      provides: "Botão PDF habilitado com seletor de projeto"
      contains: "ProjetoPDFExport"
    - path: "src/components/supcdt/ProjetoMonitoramentoModal.tsx"
      provides: "Modal reorganizado com seções colapsáveis ou agrupadas"
      contains: "Indicadores de saúde"
  key_links:
    - from: "src/components/supcdt/DetalheProjeto.tsx"
      to: "src/components/supcdt/ProjetoPDFExport.tsx"
      via: "import + renderização condicional"
      pattern: "ProjetoPDFExport"
    - from: "src/components/supcdt/Relatorios.tsx"
      to: "src/components/supcdt/ProjetoPDFExport.tsx"
      via: "import + renderização condicional"
      pattern: "ProjetoPDFExport"
---

<objective>
Implementar exportação PDF via window.print() com layout print-friendly, habilitar o botão PDF no DetalheProjeto e Relatórios, e reorganizar o ProjetoMonitoramentoModal para melhor usabilidade.

Purpose: Gestor precisa gerar relatórios institucionais em PDF para enviar a superiores e parceiros. O modal de monitoramento precisa de organização melhor para não parecer um formulário enorme.
Output: ProjetoPDFExport.tsx com layout para impressão, botões PDF habilitados em DetalheProjeto e Relatórios, ProjetoMonitoramentoModal reorganizado.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-ux-formularios-pdf/11-03-SUMMARY.md
@src/components/supcdt/DetalheProjeto.tsx
@src/components/supcdt/Relatorios.tsx
@src/components/supcdt/ProjetoMonitoramentoModal.tsx
@src/types/projeto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjetoPDFExport — layout print-friendly + wiring em DetalheProjeto e Relatórios</name>
  <files>
    src/components/supcdt/ProjetoPDFExport.tsx
    src/components/supcdt/DetalheProjeto.tsx
    src/components/supcdt/Relatorios.tsx
  </files>
  <action>
    **Abordagem: window.print() com CSS @media print** — SEM instalar nenhuma biblioteca de PDF. Esta é a abordagem mais leve e confiável para gerar PDFs no navegador. O componente renderiza uma view otimizada para impressão e invoca `window.print()`.

    **src/components/supcdt/ProjetoPDFExport.tsx** — criar componente:

    Props:
    ```typescript
    interface ProjetoPDFExportProps {
      projeto: Projeto;
      lancamentos: Lancamento[];
      onClose: () => void;
    }
    ```

    Layout: Overlay fullscreen com fundo branco, otimizado para impressão A4 (max-width: 210mm):

    1. **Header institucional** (aparece apenas no print):
       - Logo/brasão placeholder: "SECTI — Secretaria de Ciência, Tecnologia e Inovação"
       - Subtítulo: "Superintendência de Ciência, Desenvolvimento e Transferência de Tecnologia"
       - Data de geração: `new Date().toLocaleDateString('pt-BR')`

    2. **Dados do projeto** (seção):
       - Grid 2 colunas: Nome, OSC, Status, Categoria, Responsável SECTI, Parceiro, Território, Número do Termo, Processo SEI, Período (dataInicio - dataFim), Valor Total
       - Usar getters existentes (getProjetoNome, getProjetoOsc, etc.)

    3. **Monitoramento** (seção):
       - Grid 2x2: Status Operacional, Nível de Risco, Saúde da Entrega, Progresso Físico
       - Resumo executivo (texto completo)
       - Bloqueios e Próximos passos (listas)

    4. **Metas** (seção):
       - Tabela com colunas: Código, Descrição, Unidade, Previsto, Realizado, %
       - Para cada meta, se houver trimestres: sub-tabela T1-T4 com previsto/realizado

    5. **Lançamentos recentes** (seção — últimos 10):
       - Tabela: Data, Trimestre, Descrição, Local, Valores por meta

    6. **Footer** com: "Gerado automaticamente pelo Dashboard SUPCDT em {data}"

    **CSS para impressão** — adicionar `<style>` tag dentro do componente ou usar classes:
    ```css
    @media print {
      body > *:not(.pdf-export-overlay) { display: none !important; }
      .pdf-export-overlay { position: static !important; }
      .pdf-no-print { display: none !important; }
    }
    ```

    **Barra de ações (não imprime)** — classe `pdf-no-print`:
    - Botão "Imprimir / Salvar PDF" → chama `window.print()`
    - Botão "Fechar" → chama `onClose()`

    **Estilo geral**: Usar classes Tailwind com `print:` prefix onde possível. Texto preto sobre branco, sem sombras, sem arredondamentos excessivos. Tabelas com borders finas `border-collapse`.

    **src/components/supcdt/DetalheProjeto.tsx** — habilitar botão PDF:

    1. Importar `ProjetoPDFExport`
    2. Adicionar state `isPDFExportOpen: boolean`
    3. Trocar o botão disabled "PDF indisponível" por botão habilitado:
       ```tsx
       <Button
         onClick={() => setIsPDFExportOpen(true)}
         variant="outline"
         className="h-8 rounded-full border-slate-200 px-3 text-xs text-slate-700 hover:bg-slate-50"
       >
         <Download className="mr-1.5 h-3 w-3" />
         Exportar PDF
       </Button>
       ```
    4. Renderizar `ProjetoPDFExport` condicionalmente, passando `projeto` e `lancamentos`

    **src/components/supcdt/Relatorios.tsx** — habilitar botão PDF institucional:

    1. Importar `ProjetoPDFExport`
    2. O componente Relatorios recebe `projetos: Projeto[]`. Adicionar state para selecionar qual projeto exportar:
       - `selectedProjetoForPDF: Projeto | null`
    3. Trocar o item "Relatório institucional por projeto" de "Indisponível" para "Disponível":
       - Alterar ícone de `ShieldAlert` (amber) para `CheckCircle2` (emerald)
       - Alterar badge de "Indisponível" para "Disponível"
       - Alterar texto descritivo para "Selecione um projeto para gerar relatório institucional em PDF."
    4. Botão agora é habilitado: "Gerar PDF"
    5. Ao clicar, abrir um Select com lista de projetos (getProjetoNome para cada). Ao selecionar, abrir `ProjetoPDFExport`.
       - Alternativa mais simples: se só há 1 projeto, abrir direto. Se múltiplos, usar um `<select>` inline + botão "Gerar".
    6. Como Relatorios não tem `lancamentos` carregados, o `ProjetoPDFExport` receberá `lancamentos={[]}` e a seção será omitida — OU carregar lançamentos no momento da geração via `fetchLancamentosDeProjeto`.

    IMPORTANTE: NÃO instalar nenhuma biblioteca de PDF (jspdf, @react-pdf/renderer, html2canvas, etc.). Usar apenas window.print() com CSS @media print. Isso produz PDF nativo do navegador, sem dependências.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Clicar "Exportar PDF" no DetalheProjeto — overlay abre com preview do relatório. Clicar "Imprimir" — dialog de impressão do navegador abre com layout limpo A4. Selecionar "Salvar como PDF" gera um PDF bem formatado.</manual>
  </verify>
  <done>ProjetoPDFExport renderiza layout institucional completo com dados do projeto, metas, monitoramento e lançamentos; window.print() gera PDF via navegador; botão PDF habilitado no DetalheProjeto; botão PDF habilitado em Relatórios com seletor de projeto; nenhuma biblioteca de PDF instalada</done>
</task>

<task type="auto">
  <name>Task 2: Reorganizar ProjetoMonitoramentoModal com seções visuais</name>
  <files>
    src/components/supcdt/ProjetoMonitoramentoModal.tsx
  </files>
  <action>
    O ProjetoMonitoramentoModal atualmente é um formulário longo com todos os campos lado a lado. Reorganizar em seções visuais claras com separadores e títulos:

    **Seção 1: "Indicadores de saúde"** (primeira seção):
    - Header: ícone Activity + "Indicadores de saúde" (título da seção)
    - Conteúdo: Grid 2x2 com os 4 selects (Status operacional, Nível de risco, Saúde da entrega, Manutenção)
    - Checkbox "Projeto exige ação prioritária" abaixo

    **Seção 2: "Operacional"** (segunda seção):
    - Header: ícone ClipboardList + "Dados operacionais"
    - Conteúdo: Grid 1x2 com Incidentes abertos + Responsável operacional
    - Resumo executivo (textarea, full width)

    **Seção 3: "Planejamento"** (terceira seção):
    - Header: ícone ListChecks + "Planejamento e riscos"
    - Grid 2 colunas: Bloqueios (textarea) + Próximos passos (textarea)

    **Seção 4: "Território e evidências"** (quarta seção, no sidebar):
    - Header: ícone MapPin + "Território e evidências"
    - Cobertura detalhada (textarea)
    - Evidências e links (textarea)

    **Implementação das seções**:
    - Cada seção envolvida em `<div className="space-y-4">` com:
      ```tsx
      <div className="flex items-center gap-2 border-b border-slate-100 pb-2">
        <Icon className="h-4 w-4 text-sky-600" />
        <p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500">{sectionTitle}</p>
      </div>
      ```
    - Manter o layout 2 colunas (xl:grid-cols-[...]) existente mas reorganizar o conteúdo
    - Coluna esquerda: Seções 1, 2, 3
    - Coluna direita: Seção 4 + o card dark existente (Base para Wi-Fi Social)

    **Remover/atualizar texto**:
    - A descrição do header "Esta camada já prepara o modelo que será aprofundado no Wi-Fi Social." → simplificar para "Atualize indicadores, operação, planejamento e evidências do projeto."
    - O card dark "Base para o Wi-Fi Social" pode ficar, mas mover para ABAIXO das evidências na sidebar

    Importar ícones adicionais necessários: `ClipboardList`, `ListChecks` de lucide-react.

    IMPORTANTE: NÃO alterar a lógica do handleSubmit — apenas reorganizar o layout visual. Todos os estados, valores e submissão continuam idênticos.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Abrir ProjetoMonitoramentoModal — 4 seções visuais claramente separadas com ícones e títulos. Preencher campos e salvar — funciona igual antes.</manual>
  </verify>
  <done>ProjetoMonitoramentoModal tem 4 seções visuais com títulos e ícones; layout reorganizado sem scroll excessivo; lógica de submit inalterada; ícones ClipboardList e ListChecks importados</done>
</task>

</tasks>

<verification>
- Clicar "Exportar PDF" no DetalheProjeto abre overlay com layout institucional
- window.print() produz PDF limpo em A4
- Relatórios tem botão "Gerar PDF" habilitado com seletor de projeto
- ProjetoMonitoramentoModal tem 4 seções visuais claras com ícones
- Funcionalidade de salvamento do modal inalterada
- TypeScript compila sem erros
- Nenhuma biblioteca de PDF foi instalada
</verification>

<success_criteria>
- PDF export funcional via window.print() sem dependências externas
- Layout print-friendly com header institucional SECTI e dados completos do projeto
- Botões PDF habilitados em DetalheProjeto e Relatórios
- ProjetoMonitoramentoModal reorganizado com seções visuais distintas
- Zero impacto em endpoints de API
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-ux-formularios-pdf/11-04-SUMMARY.md`
</output>
