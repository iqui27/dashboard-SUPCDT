---
phase: 11-polish-ux-formularios-pdf
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/supcdt/LancamentoModal.tsx
  - src/components/supcdt/MetaModal.tsx
  - src/components/supcdt/DetalheProjeto.tsx
autonomous: true
requirements: [EDIT-02, EDIT-04, META-01]

must_haves:
  truths:
    - "Gestor consegue editar um lançamento existente (alterar valores, descrição, trimestre, data)"
    - "Gestor consegue deletar um lançamento existente com confirmação"
    - "Gestor consegue definir uma data de atividade customizada ao criar ou editar um lançamento"
    - "Gestor consegue editar o previsto por trimestre de uma meta existente e os valores persistem corretamente"
  artifacts:
    - path: "src/components/supcdt/LancamentoModal.tsx"
      provides: "Modal com modo criação e edição, date picker, botão delete"
      contains: "isEditing"
    - path: "src/components/supcdt/MetaModal.tsx"
      provides: "Modal de meta com previsto editável e funcional"
      contains: "handlePrevistoChange"
    - path: "src/components/supcdt/DetalheProjeto.tsx"
      provides: "Botões de editar e deletar em cada lançamento + botão editar em cada meta"
      contains: "editingLancamento"
  key_links:
    - from: "src/components/supcdt/LancamentoModal.tsx"
      to: "/api/lancamentos/:id"
      via: "updateLancamento/deleteLancamento from src/lib/api/lancamentos.ts"
      pattern: "updateLancamento|deleteLancamento"
    - from: "src/components/supcdt/MetaModal.tsx"
      to: "/api/projetos/:id"
      via: "updateProjeto with metas array"
      pattern: "updateProjeto.*metas"
---

<objective>
Habilitar edição e exclusão de lançamentos com data customizada + garantir que a edição de metas (previsto por trimestre) funciona corretamente.

Purpose: Gestor precisa corrigir lançamentos errados, ajustar datas retroativas e modificar previsões de metas sem recriar tudo do zero.
Output: LancamentoModal com modo edição + date picker + delete, MetaModal com previsto funcional.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-ux-formularios-pdf/11-01-SUMMARY.md
@src/components/supcdt/LancamentoModal.tsx
@src/components/supcdt/MetaModal.tsx
@src/components/supcdt/DetalheProjeto.tsx
@src/lib/api/lancamentos.ts
@src/types/projeto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar LancamentoModal — modo edição + data customizada + delete</name>
  <files>
    src/components/supcdt/LancamentoModal.tsx
    src/components/supcdt/DetalheProjeto.tsx
  </files>
  <action>
    **src/components/supcdt/LancamentoModal.tsx** — refatorar para suportar criação E edição:
    
    1. Alterar interface props para aceitar lançamento existente:
       ```typescript
       interface LancamentoModalProps {
         projeto: Projeto;
         lancamento?: Lancamento; // se presente = modo edição
         onClose: () => void;
         onSuccess: () => void;
       }
       ```
    
    2. Adicionar campo `dataAtividade` com input `type="date"`:
       - Label: "Data da atividade"
       - Posicionar ao lado do campo "Local da atividade" no grid
       - Default: data de hoje em formato YYYY-MM-DD
       - No modo edição: inicializar com `lancamento.dataAtividade` se existir
    
    3. Modo edição:
       - Header muda de "Novo lançamento" para "Editar lançamento"
       - Inicializar todos os campos com valores do lançamento existente:
         - `trimestre` → `lancamento.trimestre`
         - `descricaoAtividade` → `lancamento.descricaoAtividade`
         - `localAtendido` → `lancamento.localAtendido`
         - `valores` → map de `lancamento.valores` ({metaId: valorRealizado})
         - `dataAtividade` → `lancamento.dataAtividade` (se existir)
       - Submit chama `updateLancamento(lancamento.id, payload, token)` em vez de `criarLancamento`
    
    4. Botão de deletar (somente no modo edição):
       - Botão vermelho "Excluir" no footer à esquerda (antes do Cancelar)
       - Ao clicar, mostrar `window.confirm('Tem certeza que deseja excluir este lançamento?')`
       - Se confirmado, chamar `deleteLancamento(lancamento.id, token)` + toast success + onSuccess()
    
    5. No submit de criação, incluir `dataAtividade` no payload se preenchido.
    
    **src/components/supcdt/DetalheProjeto.tsx** — na seção de lançamentos:
    
    1. Adicionar state `editingLancamento: Lancamento | null`
    2. Em cada `<li>` de lançamento, adicionar botão de editar (ícone Pencil):
       ```
       <button onClick={() => setEditingLancamento(lanc)} className="rounded-full p-1.5 text-slate-400 hover:bg-slate-100">
         <Pencil className="h-3.5 w-3.5" />
       </button>
       ```
    3. Renderizar `LancamentoModal` quando `editingLancamento` não for null, passando o lançamento como prop
    4. No onSuccess, fazer `setEditingLancamento(null)` + `carregarLancamentos()` + `onUpdate()`
    5. Se `lancamento.dataAtividade` existir, exibir junto à data de registro na lista de lançamentos
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Clicar editar em um lançamento, alterar descrição, salvar — valor atualiza. Clicar excluir — lançamento removido e metas recalculadas.</manual>
  </verify>
  <done>LancamentoModal funciona em modo criação (como antes) e edição (com dados preenchidos); campo data de atividade aparece em ambos os modos; botão excluir funciona no modo edição; lista de lançamentos no DetalheProjeto tem botões de editar; data de atividade exibida quando presente</done>
</task>

<task type="auto">
  <name>Task 2: Verificar e corrigir edição de previsto por trimestre na MetaModal</name>
  <files>
    src/components/supcdt/MetaModal.tsx
    src/components/supcdt/DetalheProjeto.tsx
  </files>
  <action>
    **Diagnóstico**: O MetaModal já tem inputs para `previstoPorTrimestre`, mas o usuário reporta que não consegue editar. Investigar:
    
    1. Verificar se o `meta?.previstoPorTrimestre` chega undefined ao MetaModal no modo edição — pode ser que o backend/normalização não retorne esse array
    2. Verificar se `updateProjeto` no submit sobrescreve todo o projeto (incluindo campos de módulos) — pode estar perdendo dados
    3. Verificar se o array `previsto` state está sincronizado com `totalTrimesters`
    
    **Correções a fazer no MetaModal.tsx**:
    
    1. Garantir que o estado `previsto` tem o tamanho correto:
       ```typescript
       const initialPrevisto = meta?.previstoPorTrimestre ?? [];
       // Pad to totalTrimesters if shorter
       while (initialPrevisto.length < totalTrimesters) initialPrevisto.push(0);
       const [previsto, setPrevisto] = useState<number[]>(initialPrevisto.slice(0, totalTrimesters));
       ```
    
    2. No handleSubmit do modo edição, garantir que `realizadoTotal` e `realizadoPorTrimestre` são PRESERVADOS da meta original (não zerados):
       ```typescript
       novasMetas = projeto.metas.map((m) =>
         m.id === meta.id
           ? { ...m, codigo, descricao, unidade, totalPrevisto, previstoPorTrimestre: previsto }
           : m
       );
       ```
       (o spread `...m` já faz isso, mas verificar que não há override acidental)
    
    3. No submit, enviar APENAS os campos necessários para a atualização, não o projeto inteiro:
       ```typescript
       await updateProjeto(projeto.id, { metas: novasMetas }, token);
       ```
       Em vez de `{ ...projeto, metas: novasMetas }` — que pode sobrescrever campos de módulos com valores stale.
    
    4. Adicionar validação: `totalPrevisto` deve ser > 0, mostrar toast de aviso se for 0.
    
    **DetalheProjeto.tsx** — pequena melhoria na exibição de metas:
    
    1. Se `meta.realizadoTotal > meta.totalPrevisto && meta.totalPrevisto > 0`:
       - Trocar a cor da barra de progresso de `bg-sky-600` para `bg-emerald-500`
       - Alterar o texto do percentual para incluir nota: `"Meta atingida (120%)"` ou similar
       (Nota: este é um ajuste preparatório — o badge completo de "Meta Superada" será feito no Plan 11-03)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Abrir MetaModal em modo edição para uma meta com previsto preenchido — valores T1-T4 devem aparecer. Alterar T2, salvar — valor T2 persiste ao reabrir. Metas existentes (realizado) não são zeradas após editar previsto.</manual>
  </verify>
  <done>MetaModal em modo edição mostra valores previsto corretos; alterar previsto e salvar persiste o valor; realizadoTotal e realizadoPorTrimestre não são perdidos na edição; barra de progresso muda de cor quando meta é superada; totalPrevisto valida que é > 0</done>
</task>

</tasks>

<verification>
- Criar lançamento com data customizada → lançamento mostra data de atividade na lista
- Editar lançamento → alterar descrição → salvar → texto muda
- Deletar lançamento → metas do projeto são recalculadas automaticamente
- Editar meta → alterar T2 previsto → salvar → reabrir modal → T2 mostra novo valor
- Editar meta → realizado total NÃO é zerado
- TypeScript compila sem erros
</verification>

<success_criteria>
- LancamentoModal funciona em modo criação e edição
- Campo data de atividade presente e funcional
- Botão excluir com confirmação remove lançamento e recalcula metas
- MetaModal preserva previsto por trimestre na edição
- Barra de progresso muda de cor quando meta superada
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-ux-formularios-pdf/11-02-SUMMARY.md`
</output>
